require File.dirname(__FILE__) + "/../test_helper"

class HolidayTest < ActiveSupport::TestCase

	def create_holiday(attributes = {})
	
		attributes[:startdate] ||=	
		attributes[:enddate] ||=
		attributes[:amountofdays] ||=
		attributes[:user_id] ||=
		holiday = Holiday.new(attributes)
		holiday
	end
  
end
